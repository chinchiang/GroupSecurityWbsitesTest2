# DEMO_STATIC does not require runtime secrets.
# Production names are placeholders only; never place real values here.
VITE_DEPLOYMENT_PROFILE=DEMO_STATIC
OIDC_ISSUER_URL=https://identity.invalid/example
SESSION_COOKIE_NAME=__Host-group-security-session
